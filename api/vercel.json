{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "config": { 
        "includeFiles": ["../package.json"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/",
      "methods": ["GET", "POST", "OPTIONS"]
    }
  ]
}